enum AuthLoginStatus { loading, notloading }

enum AuthSignupStatus { loading, notloading }

enum CategoryStatus { DONE, LOADING, NIL }

enum ProductStatus { DONE, LOADING, NIL }

enum UserStatus { SET, LOADING, NOTSET }

enum CartStatus { DONE, LOADING, NIL }

enum OrderStatus { DONE, LOADING, NIL }

enum WishListStatus { DONE, LOADING, NIL }

enum ProductFetchStatus { DONE, LOADING, NIL }